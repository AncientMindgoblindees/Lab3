# Resumes Directory

This directory stores uploaded PDF resumes for each profile.

Files are named as `{profile}.pdf` (e.g., `chris.pdf`, `tj.pdf`).

Resumes are uploaded via the Admin Dashboard (protected.html) and displayed on each profile page.
